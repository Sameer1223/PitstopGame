# how to create a new car type

In your inspector, right-click -> Create -> Racing -> Car Stats.

Give the created scriptable object a name and update the values.

Create a duplicate of the existing car prefab, and drag the newly created scriptable object into the `BaseStats`
value for `CarStatsRuntime`.